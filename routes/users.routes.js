import express from 'express';

const router = express.Router();

// routes.use("/login", authService);

// Add your user routes here
// Example: router.get('/', getAllUsers);

export default router;
